{# {% extends 'base.html.twig' %}

{% block title %}Créneaux{% endblock %}

{% block body %}
{% if not app.user or 'ROLE_ADMIN' not in app.user.roles %}
        <div class="alert alert-danger" role="alert">
            Vous n'avez pas les droits d'accéder à cette page.
        </div>
{% else %}
   <div class="MinWidthGeneral" >
        <div class="table-responsive showCreneaux">
            <div class="creneauShowH4">
               <h4 id="h4Creneau">Show Détail</h4>
            </div>
            <table class="table">
                <tbody>
                    <tr>
                        <th scope="row">Id</th>
                        <td>{{ creneaux.id }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Date</th>
                        <td>{{ creneaux.date ? creneaux.date|date('Y-m-d H:i:s') : '' }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Moniteur</th>
                        <td>{{ creneaux.user.firstname }}</td>
                    </tr>
                    <tr>
                        <th scope="row">Permis Type</th>
                        <td>{{ creneaux.permis.type }}</td>
                    </tr>
                    <tr>
                        <th scope="row">IsAvailable</th>
                        <td>{{ creneaux.isAvailable ? 'Yes' : 'No' }}</td>
                    </tr>
                </tbody>
            </table>
            <a class="btn btn-secondary" href="{{ path('app_creneaux_index') }}"><span>&#128412;</span>List</a>
                 <a class="btn btn-secondary" href="{{ path('app_creneaux_edit', {'id': creneaux.id}) }}">Edit</a>
        </div>
  </div>
 {{ include('creneaux/_delete_form.html.twig') }}
  {% include 'footer.html.twig' %}
 {% endif %}
{% endblock %} #}
